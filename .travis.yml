language: rust
sudo: required

rust:
  - stable
  - beta
  - nightly

os:
  - linux

cache: cargo

matrix:
  allow_failures:
    - rust: nightly

script:
  - cargo test -vv

addons:
  apt:
    sources:
      - kubuntu-backports

    packages:
      - cmake
      - gfortran
