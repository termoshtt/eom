box: termoshtt/rust-dev:latest

test-static:
  steps:
    - script:
      name: test static links (OpenBLAS/FFTW)
      code: cargo test --no-default-features --features=static

test-intel-mkl:
  steps:
    - script:
      name: test Intel MKL backend
      code: cargo test --no-default-features --features=intel-mkl

